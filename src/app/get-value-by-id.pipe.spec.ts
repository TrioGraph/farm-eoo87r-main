import { GetValueByIdPipe } from './get-value-by-id.pipe';

describe('GetValueByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetValueByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
